LEVEL 10 - ANÁLISIS EXHAUSTIVO
================================

CÓDIGO VULNERA:
$file = $_REQUEST['f'];
$request = $_REQUEST['hash'];
$hash = substr(md5($flag . $file . $flag), 0, 8);
if ($request == $hash) {
    show_source($file);
}

PROBLEMAS IDENTIFICADOS:
1. Comparación débil (==) permite type juggling
2. Hash de solo 8 caracteres (32 bits) es débil
3. No conozco $flag para calcular el hash correcto

TÉCNICAS PROBADAS (TODAS FALLARON):
✗ Parameter pollution (GET/POST/COOKIE combinations)
✗ Array manipulation
✗ Path traversal / null bytes
✗ PHP wrappers (php://filter)
✗ Type juggling con hash "0" y paths "0e..."
✗ Búsqueda de colisiones MD5 parciales
✗ Bruteforce de hashes (demasiado lento)
✗ Multipart form data
✗ Envío de hash como array
✗ Intentos de causar errores que divulguen info

HASH CONOCIDO:
- index.php: b4382d64

EL RETO PARECE REQUERIR:
- O conocer/adivinar $flag
- O encontrar colisión de 32 bits (factible pero requiere mucho tiempo)
- O alguna técnica que AÚN no he descubierto

SIGUIENTE INTENTO:
Voy a crear un bruteforce MÁS INTELIGENTE, probando solo hashes que sean "0e..." para explotar type juggling...