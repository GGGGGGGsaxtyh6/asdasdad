
include "flag.php";include "utils.php";include "session_management.php";if (isset($_POST['username']) and isset($_POST['password'])) {    if (isset($_POST['register'])) {        $message = register($_POST['username'], $_POST['password']);    } elseif (isset($_POST['login'])) {        $message = login($_POST['username'], $_POST['password']);    }} elseif (isset ($_POST['logout'])) {    setcookie("session", "", time() - 3600);    die ('');}?>    #WebSec Level LevelTwentyOne                                        LevelTwentyOne - PatPat                                            For security reason, you're not allowed to register using the letters [admin].                You can get the sources of the index here,                the one for sessions management here, and the crypto                stuff here.                Many thanks to icernica for this level.                                        if (isset ($message)): ?>                    echo $message[0]; ?> alert-dismissible" role="alert">                                    &times;                                echo $message[1]; ?>                            endif; ?>        if (isset ($_COOKIE['session'])) :?>                                                Welcome echo auth($_COOKIE['session'], $aes_key) === "admin"? 'admin': 'user'; ?>                                                    if (auth($_COOKIE['session'], $aes_key) === "admin") {                        echo 'Hello admin, here is your flag: ' . $flag;                        $pdo = new SQLite3 ('database.db', SQLITE3_OPEN_READWRITE);                        $stmt = $pdo->prepare('DELETE FROM users WHERE ip = ":ip";');                        $stmt->bindValue(':ip', $_SERVER['REMOTE_ADDR'], SQLITE3_TEXT);                        $stmt->execute();                    } else {                        echo 'You need to be admin to see the flag';                    }                    ?>                                                                            logout                                                else:  // No cookie, the person isn't logged. ?>                                                Username                                                                        Password                                                                        Login                    Register                            endif ?>            

