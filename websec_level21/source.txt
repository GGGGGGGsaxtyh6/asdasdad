include "flag.php""utils.php""session_management.php"$_POST['username']) and isset($_POST['password'$_POST['register'$message = register($_POST['username'], $_POST['password'$_POST['login'$message = login($_POST['username'], $_POST['password'$_POST['logout'setcookie("session", "", time() - 3600'<meta http-equiv="refresh" content="0">'<?php if (isset ($message)): <?php echo $message[0]; ?><?php echo $message[1]; <?php endif; <?php if (isset ($_COOKIE['session'])) :<?php echo auth($_COOKIE['session'], $aes_key) === "admin"? 'admin': 'user'; ?><?php if (auth($_COOKIE['session'], $aes_key) === "admin"'Hello admin, here is your flag: ' . $flag$pdo = new SQLite3 ('database.db', SQLITE3_OPEN_READWRITE$stmt = $pdo->prepare('DELETE FROM users WHERE ip = ":ip";'$stmt->bindValue(':ip', $_SERVER['REMOTE_ADDR'], SQLITE3_TEXT$stmt->execute'You need to be admin to see the <mark>flag</mark>'<?php else:  // No cookie, the person isn't logged. <?php endif 
